spring.application.name=selfinvestment
server.port=8080

# API key to protect endpoints (change before deploying)
app.security.api-key=akhilsharma

# CORS: comma-separated allowed origins
cors.allowed.origins=http://localhost:8080